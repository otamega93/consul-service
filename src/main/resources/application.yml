server:
  port: 4469
spring:
  application:
    name: consul-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        instanceId: ${spring.application.name}
      config:
        enabled: true
management:
  endpoints:
    web:
      exposure:
         #include: "*"
         include: refresh
